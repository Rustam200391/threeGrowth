
interface threeParametrs {
    oil: string,
    years: number
}

const threeGrowth = ({ oil: string, years: number }:threeParametrs):JSX.Element => {
    let count = name string,

    for (let i = 0; i < threeGrowth.length; i++) {
        if (count ** i) {
            let threeLenght = years:number ** threeGrowth:string;
        }
}